# Mintos testa uzdevums

## Setup

1. git clone https://github.com/kristaps-soloveiko/rss.git
2. composer install
3. cp ./.env_example ./.env
4. Update sqlite database path in .env
5. php artisan serve
